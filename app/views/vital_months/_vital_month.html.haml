%table.table.table-hover
  %thead
    %tr
      %th 
      %th 体温
      %th 高血圧
      %th 低血圧
      %th 脈拍
      %th 呼吸数
      %th 酸素飽和度(SpO2)
      %th{:colspan => "3"}
  - vitals.each do |vital|
    %tbody
      %tr{:id => "vital_#{vital.id}"}
        %td= vital.vital_created_at
        %td{:class => "#{check_temprature(vital)}"}= vital.temperature
        %td{:class => "#{check_high_bp(vital)}"}= vital.high_bp
        %td{:class => "#{check_low_bp(vital)}"}= vital.low_bp
        %td{:class => "#{check_pulse(vital)}"}= vital.pulse
        %td{:class => "#{check_breathing(vital)}"}= vital.breathing
        %td{:class => "#{check_oxygen_saturation(vital)}"}= vital.oxygen_saturation